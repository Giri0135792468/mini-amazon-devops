apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: product-service

  ports:
    - port: 5001
      targetPort: 5001
      protocol: TCP

  type: ClusterIP