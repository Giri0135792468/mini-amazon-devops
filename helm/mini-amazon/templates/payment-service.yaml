apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: payment-service

  ports:
    - port: 5005
      targetPort: 5005
      protocol: TCP

  type: ClusterIP